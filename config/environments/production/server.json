{
  "host": "localhost",
  "port": "${process.env.PORT || 1337}",
  "proxy": {
    "enabled": true,
    "ssl": true,
    "host": "${process.env.HOST}"
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "path": "/admin"
  }
}
